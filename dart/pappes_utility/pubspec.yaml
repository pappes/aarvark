name: pappes_utility
version: 1.0.3
description: A library useful for console applications or web applications.  Currently includes RamCache which is an in memory object store and helper functions like ifNull()
homepage: https://github.com/pappes/aarvark
dev_dependencies:
  lints: ^1.0.1
  test: ^1.20.2
environment:
  sdk: '>=2.12.0 <3.0.0'

