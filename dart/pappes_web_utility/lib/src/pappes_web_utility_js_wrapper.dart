// Copyright (c) 2015, Pappes. All rights reserved. Use of this source code
// is governed by a BSD-style license that can be found in the LICENSE file.

import 'package:js/js.dart';

// @JS()
// class JSWrapper {
// // Calls invoke JavaScript `eval(command)`.
//   @JS('eval')
//   external Object? eval(String command);
// // Calls invoke JavaScript `window.setTimeout(function,0)`.
//   @JS('window.setTimeout')
//   external int setTimeout(String function);
// // Calls invoke JavaScript `window.clearTimeout(obj)`.
//   @JS('window.clearTimeout')
//   external void clearTimeout(int timer);
// // Calls invoke JavaScript `window.atob(text)`.
//   @JS('window.atob')
//   external String atob(String text);
// // Calls invoke JavaScript `window.btoa(text)`.
//   @JS('window.btoa')
//   external String btoa(String text);
// }
